import React from "react"

const Square = (props) => {
    return(
        <div style = {{backgroundColor : props.color, width: 300, height: 300, border: '3px solid black'}}>

        </div>
    )
}

export default Square